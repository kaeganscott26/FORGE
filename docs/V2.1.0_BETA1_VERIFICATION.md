# FORGE beta 2.1 Verification

**Release identity:** `2.1.0-beta.1`

**Public name:** `FORGE beta 2.1`

**Baseline:** `8350aab`

**Target tag:** `v2.1.0-beta.1`

**Date:** August 7, 2026

## Scope

This release resets the broken post-`8350aab` line and repairs the interactive workspace explorer, text-file editing, model discovery, action reporting, and responsive layout behavior.

## Source validation

| Check | Result |
| --- | --- |
| `npm run typecheck` | Passed |
| `npm run lint` | Passed |
| `npm test` | Passed — 25 files / 99 tests |
| `npm run build` | Passed |
| `git diff --check` | Passed |

## Feature evidence

- Workspace tests exercise recursive tree listing, arbitrary UTF-8 extension reads, writes, and copy without overwrite.
- Renderer tests exercise normalized paths, parent/child explorer paths, and collision-safe copy names.
- Explorer supports folder expansion/collapse, context actions, new file/folder, rename, delete, copy, paste, and keyboard shortcuts.
- The editor opens unknown UTF-8 text extensions as plain text and maps major coding languages to Monaco grammars.
- Saved remote credentials and loopback providers automatically request model catalogs; manual refresh and exact model validation remain available.
- Layout values are clamped when loaded, resized, and persisted; narrow windows hide secondary panels to protect editor readability.

## Release gates still requiring independent evidence

- Final pushed commit and annotated tag target.
- GitHub Actions packaging result and universal DMG/ZIP/blockmap/`beta-mac.yml` hashes.
- Packaged `file://` runtime acceptance and installed `/Applications/FORGE.app` smoke test.
- Public artifact re-download, extraction, updater discovery, and manual installation.
- Developer ID signing and notarization, which are not configured and therefore cannot be claimed.
